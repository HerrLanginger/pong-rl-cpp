#pragma once
#include "raylib.h"

namespace Config
{
    static const int ScreenWidth = 1280;
    static const int ScreenHeight = 800;
    static const int TargetFPS = 60;
    static const char* Title = "really PONG?";

    static const float PaddleSpeed = 500.0f;
    static const float BallSpeed = 400.0f;
}